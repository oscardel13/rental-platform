name: Rental Platform Prod CI/CD

on:
  push:
    branches: [main]

jobs:
  build_server:
    name: Build and Push Server Image
    runs-on: ubuntu-latest

    env:
      IMAGE_NAME: ${{ secrets.DOCKERHUB_USERNAME }}/rental-platform-server

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push server image
        uses: docker/build-push-action@v6
        with:
          context: ./server
          file: ./server/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:latest
            ${{ env.IMAGE_NAME }}:${{ github.sha }}

  deploy_server:
    name: Deploy Server to EC2
    runs-on: ubuntu-latest
    needs: build_server

    steps:
      - name: SSH into EC2 and deploy
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e

            cd /opt/rental-platform

            docker login -u ${{ secrets.DOCKERHUB_USERNAME }} -p ${{ secrets.DOCKERHUB_TOKEN }}

            docker compose -f docker-compose.prod.yml pull

            docker compose -f docker-compose.prod.yml run --rm migrate

            docker compose -f docker-compose.prod.yml up -d server

            docker image prune -f

  # deploy_dashboard:
  #   name: Build and Deploy Dashboard to S3
  #   runs-on: ubuntu-latest
  #   needs: build_server
  #
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Set up Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: npm
  #         cache-dependency-path: dashboard/package-lock.json
  #
  #     - name: Install dashboard dependencies
  #       run: |
  #         cd dashboard
  #         npm ci
  #
  #     - name: Build dashboard
  #       run: |
  #         cd dashboard
  #         npm run build
  #       env:
  #         NEXT_PUBLIC_API_URL: ${{ secrets.NEXT_PUBLIC_API_URL }}
  #         NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY }}
  #
  #     - name: Configure AWS credentials
  #       uses: aws-actions/configure-aws-credentials@v4
  #       with:
  #         aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
  #         aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  #         aws-region: ${{ secrets.AWS_REGION }}
  #
  #     - name: Deploy dashboard to S3
  #       run: |
  #         aws s3 sync dashboard/out s3://${{ secrets.S3_DASHBOARD_BUCKET }} --delete
  #
  #     - name: Invalidate CloudFront cache
  #       run: |
  #         aws cloudfront create-invalidation \
  #           --distribution-id ${{ secrets.CLOUDFRONT_DASHBOARD_DISTRIBUTION_ID }} \
  #           --paths "/*"
